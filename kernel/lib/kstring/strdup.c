#include <kstring.h>





char *strdup(const char *s) {
    // size_t length = 0;

    // if (!s) {
    //     return NULL;
    // }

    // length = strlen(s) + 1;

    // char *n = kmalloc(length);

    // if (!n) {
    //     return NULL;
    // }

    // strncpy(n, s, length);
    // return n;
}